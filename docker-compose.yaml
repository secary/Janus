
services:
  web:
    build:
      context: .
      dockerfile: ./dockerfile.javelin
    image: javelin
    container_name: web
    ports:
      - "5024:5000"
    env_file:
      - .env.prod
    restart: always