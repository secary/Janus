# 项目协作要求

## 当前业务目标

本应用覆盖以下链路：

`中国银行汇率抓取 -> 历史数据入库 -> 时序模型训练 -> 汇率预测 -> 预测结果入库 -> API 与前端展示`

中国银行公开页面是当前汇率数据来源。应用负责按照既定币种和采集频率抓取数据，校验入库所需字段，并将有效数据持久化到数据库。历史数据是模型训练、预测及页面展示的统一数据基础。

预测任务读取数据库中的历史汇率数据，按照固定时间粒度完成预处理，并生成未来汇率预测。当前实现使用 LSTM 模型；后续可在保持历史数据、预测结果表和展示链路兼容的前提下，引入 Chronos 等预训练时序模型，并通过回测结果决定是否替换现有模型。

汇率预测链路的目标是：

1. 按配置的币种和时间范围读取有效历史汇率数据。
2. 按既定时间粒度完成重采样、缺失值处理和模型输入构造。
3. 训练或加载对应预测模型，生成指定预测周期内的汇率结果。
4. 将预测结果持久化到 `prediction` 表，并由 Spring Boot API 和前端统一展示。
5. 保证本地运行、Docker 部署和定时任务使用一致的目录、配置与执行入口。

## 当前 GAP

1. [ ] 根据真实端到端运行结果继续修正汇率抓取、数据库写入、模型训练、预测和前端展示中的运行时问题。
2. [ ] 对现有 LSTM 与 Chronos 进行统一口径的历史回测，比较准确率、预测耗时和资源占用，再决定生产预测模型的切换方案。
3. [ ] 接入 Chronos 预测实现，并保持现有 `history`、`prediction` 数据结构及 API/前端展示链路兼容。
4. [ ] 梳理模型文件、下载缓存和预测产物的持久化策略，减少容器重建对模型运行的影响。

## 开发原则

- 每次执行用户指示的任务前，先阅读 `README.md` 和 `doc/SPRINT.md`，确认当前业务目标、GAP 和迭代状态；文件不存在时应明确说明，不得假定其中内容。
- 除非用户明确指示修改 `README.md`，否则不得擅自新增、删除或调整 `README.md` 中的内容。
- 数据库持久化属于正式业务流程，默认且必须执行；数据库连接或写入失败时应明确报错并停止当前处理，不得静默跳过入库。
- 历史汇率数据以数据库 `history` 表为统一来源，预测结果以 `prediction` 表为统一输出；不得在未明确迁移方案的情况下绕开现有数据链路。
- 保持汇率抓取、数据预处理、模型训练、预测、数据库访问和 API/前端展示之间的职责边界清晰。
- 预测频率、历史窗口和预测周期必须与当前数据采集频率及页面展示口径一致；调整时应同步检查定时任务和数据消费端。
- 引入新模型时优先复用现有数据读取、预处理、预测结果写入和展示流程，不做与当前需求无关的抽象或重构。
- 模型效果判断应基于相同时间范围、相同预测步长和相同评估指标的回测结果，不以单次预测结果替代评估。
- 优先沿用现有项目结构、字段名称和处理流程，不做与当前需求无关的目录调整或技术栈替换。
- 每次收到指令后直接围绕问题检查并修改代码，不进行无关的发散设计。
- 每次新增或调整 GAP 时，必须根据当前业务目标和依赖关系重新按优先级排序；编号 `1` 为最高优先级，编号越大优先级越低。
- 新增 Python 文件原则上不超过 300 行。修改现有超长文件时不得继续无边界扩张；仅在职责边界明确且与当前任务直接相关时拆分模块，不为满足行数限制进行无关重构。
- 本地命令、Docker 镜像、Compose volume、cron 和文档中的预测目录统一使用 `forecasting/`，不得重新引入旧的 `predictor/` 路径。

## 测试要求

- 完成代码后不要主动运行测试。
- 仅对包含独立业务逻辑的 Python 脚本补充或更新 `test` 目录中的单元测试，由用户自行运行验证。
- 单元测试原则上按业务脚本组织：一个包含独立业务逻辑的 Python 文件对应一个同名测试文件。
- 汇率抓取规则、数据预处理、序列构造、预测结果转换和模型评估等业务逻辑应在各自测试文件中验证。
- 入口、配置、兼容转发以及不包含业务判断的封装脚本不要求建立对应测试；业务编排脚本仅测试流程衔接，具体业务规则在其所属脚本的测试文件中验证。
- 数据库相关测试不得通过静默跳过入库来模拟成功；需要隔离时应使用明确的测试数据库或 mock，并验证连接和写入失败能够终止处理。
- 部署配置、数据库初始化或升级脚本，以及其他运维服务不要求编写测试。
- 在最终说明中明确列出新增或调整的测试文件，并注明未执行测试。

## 迭代要求

- 每 5 条用户指令形成一个 Sprint 迭代，并在 `doc/SPRINT.md` 中记录目标、已完成事项和遗留事项。
- `doc/SPRINT.md` 不存在时，在首次需要记录 Sprint 时创建，并从当前可确认的用户指令开始记录，不虚构此前迭代历史。
- 不足 5 条时继续记录在当前 Sprint，不提前结束迭代。
- Sprint 记录只描述实际完成的工作，不把计划、建议或推测写成已完成。
- Sprint 中的遗留事项应与当前 GAP 对齐；完成、删除或调整 GAP 后同步更新优先级和对应迭代记录。
